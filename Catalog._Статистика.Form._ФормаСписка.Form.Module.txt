
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//Элементы.ПоказыватьТолькоИспользуемые.Заголовок = "Показывать все";
	//флЕстьОтбор = Ложь;
	//Для Каждого Элем Из Список.Отбор.Элементы Цикл 
	//	Если Список.Отбор.Элементы[0].ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Использовать") Тогда
	//		Элем.Использование  = Истина;
	//		флЕстьОтбор = Истина;	
	//	КонецЕсли;
	//КонецЦикла;	
	//
	//Если Не флЕстьОтбор Тогда  //ЭлементОтбораКомпоновкиДанных
	//	Элем = Список.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	//	Элем.Использование  = Истина;
	//	Элем.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Использовать");
	//	Элем.ПравоеЗначение = Истина;
	//КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПоказыватьТолькоИсп(Команда)
	
	//Если Элементы.ПоказыватьТолькоИспользуемые.Заголовок = "Показывать только используемые" Тогда
	//	флЕстьОтбор = Ложь;
	//	Для Каждого Элем Из Список.Отбор.Элементы Цикл 
	//		Если Список.Отбор.Элементы[0].ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Использовать") Тогда
	//			Элем.Использование  = Истина;
	//			флЕстьОтбор = Истина;	
	//		КонецЕсли;
	//	КонецЦикла;	
	//	
	//	Если Не флЕстьОтбор Тогда 
	//		Элем = Список.Отбор.Элементы.Добавить;
	//		Элем.Использование  = Истина;
	//		Элем.ЛевоеЗначение  = Новый ПолеКомпоновкиДанных("Использовать");
	//		Элем.ПравоеЗначение = Истина;
	//	КонецЕсли;
	//	Элементы.ПоказыватьТолькоИспользуемые.Заголовок = "Показывать все";
	//Иначе
	//	Элементы.ПоказыватьТолькоИспользуемые.Заголовок = "Показывать только используемые";
	//	Для Каждого Элем Из Список.Отбор.Элементы Цикл 
	//		Если Список.Отбор.Элементы[0].ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Использовать") Тогда
	//			Элем.Использование  = Ложь;
	//		КонецЕсли;
	//	КонецЦикла;
	//КонецЕсли;
	
КонецПроцедуры
